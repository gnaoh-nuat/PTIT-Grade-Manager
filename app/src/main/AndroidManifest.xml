<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PTITGradeManager"
        tools:targetApi="31">

        <!-- Activity Đăng nhập sẽ là màn hình khởi chạy ứng dụng -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Khai báo các Activity chung khác -->
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgotPasswordActivity" />

        <!-- Khai báo các Activity dành cho Sinh viên -->
        <activity android:name=".Student.activities.StudentHomepage" />

        <!-- Khai báo các Activity dành cho Giáo viên -->
        <activity android:name=".Teacher.activities.TeacherHomepage" />

    </application>

</manifest>
